int calc(int* arr, int size)  
{  

      int ans = 0;  
      for (int i = 1; i < size; i++)  
      {  
         ans += arr[i] - arr[i-1];  
      }  
      return ans;  
}  

Алгоритмическая сложность О(n).
Проходим один раз и пропорционально количеству.

Доппамять константная О(1).
Задействованы переменные  ans, arr[i], arr[i-1].



